#include <iostream>

void showArray(int array[], int size)
{
    for (int i = 0; i < size; i++)
        std::cout<<array[i]<<" ";
}

const int SIZE = 6;

int main()
{
    int numbers[SIZE] = {31, 22, 43, 56, 12, 65};
    showArray(numbers, SIZE);;
      
    return 0;
}
